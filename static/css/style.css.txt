 	* {
        box-sizing: border-box;
    }
    
    html {
        scroll-behavior: smooth;
    }
    
    body {
        margin: 0;
        font-family: "Segoe UI", Arial, sans-serif;
        background: #f4f7fb;
        color: #0f172a;
    }
    
    .layout {
        display: flex;
        min-height: 100vh;
    }
    
    .sidebar {
        width: 260px;
        background: linear-gradient(180deg, #071b33, #061226);
        color: white;
        padding: 24px 18px;
        display: flex;
        flex-direction: column;
        gap: 24px;
        position: sticky;
        top: 0;
        height: 100vh;
        z-index: 1000;
    }
    
    .brand {
        display: flex;
        align-items: center;
        gap: 12px;
    }
    
    .brand-icon {
        width: 42px;
        height: 42px;
        border-radius: 12px;
        background: #0ea5e9;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 900;
    }
    
    .brand h2 {
        margin: 0;
        letter-spacing: 1px;
    }
    
    .brand span {
        font-size: 12px;
        opacity: 0.72;
    }
    
    .sidebar-nav {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }
    
    .nav-link {
        padding: 13px 14px;
        border-radius: 12px;
        color: #dbeafe;
        text-decoration: none;
        cursor: pointer;
        font-weight: 700;
        opacity: 0.9;
        transition: 0.2s ease;
        user-select: none;
    }
    
    .nav-link.active,
    .nav-link:hover {
        background: linear-gradient(90deg, #14b8a6, #0ea5e9);
        color: white;
    }
    
    .sidebar-footer {
        margin-top: auto;
        background: rgba(255,255,255,0.08);
        border-radius: 14px;
        padding: 14px;
    }
    
    .sidebar-footer span {
        display: block;
        font-size: 12px;
        opacity: 0.7;
    }
    
    .sidebar-footer strong {
        color: #22c55e;
    }
    
    .main {
        flex: 1;
        padding: 24px;
        overflow-y: auto;
    }
    
    .topbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 22px;
        gap: 20px;
    }
    
    .topbar h1 {
        margin: 0;
        font-size: 30px;
    }
    
    .topbar p {
        margin: 6px 0 0;
        color: #64748b;
    }
    
    .top-actions {
        display: flex;
        align-items: center;
        gap: 14px;
    }
    
    .date-box,
    .icon-btn {
        background: white;
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        padding: 12px 16px;
        box-shadow: 0 8px 18px rgba(15,23,42,0.06);
    }
    
    .icon-btn {
        width: 46px;
        height: 46px;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 18px;
        margin-bottom: 18px;
    }
    
    .stat-card {
        background: white;
        border-radius: 18px;
        padding: 20px;
        border: 1px solid #e2e8f0;
        box-shadow: 0 10px 25px rgba(15,23,42,0.06);
    }
    
    .stat-card span {
        color: #64748b;
        font-weight: 800;
        font-size: 14px;
    }
    
    .stat-card h3 {
        font-size: 34px;
        margin: 12px 0 6px;
    }
    
    .stat-card small {
        color: #16a34a;
        font-weight: 700;
    }
    
    .stat-card.alarm-card h3,
    .stat-card.alarm-card small {
        color: #dc2626;
    }
    
    .panel {
        background: white;
        border-radius: 20px;
        padding: 20px;
        border: 1px solid #e2e8f0;
        box-shadow: 0 10px 25px rgba(15,23,42,0.07);
        scroll-margin-top: 24px;
    }
    
    .panel-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 14px;
        gap: 12px;
    }
    
    .panel h2 {
        margin: 0;
        font-size: 20px;
    }
    
    .panel p {
        margin: 5px 0 0;
        color: #64748b;
        font-size: 13px;
    }
    
    .badge {
        background: #dcfce7;
    color: #166534;
    border-radius: 999px;
    padding: 8px 12px;
    font-size: 12px;
    font-weight: 900;
    white-space: nowrap;
}

.badge.dark {
    background: #0f172a;
    color: white;
}

.content-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 18px;
    margin-bottom: 18px;
}

.right-column {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.lower-grid {
    display: grid;
    grid-template-columns: 1.25fr 1fr 1fr;
    gap: 18px;
    margin-bottom: 18px;
}

#cameraFeed {
    width: 100%;
    min-height: 390px;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    border-radius: 16px;
    border: 1px solid #e2e8f0;
    background: #020617;
}

.camera-buttons {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
}

.camera-btn {
    border: none;
    background: #e2e8f0;
    color: #0f172a;
    font-weight: 900;
    padding: 10px 13px;
    border-radius: 10px;
    cursor: pointer;
}

.camera-btn.active {
    background: #0ea5e9;
    color: white;
}

.detections-list {
    margin-top: 14px;
}

.detections-list div {
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 10px;
    margin-bottom: 8px;
    font-size: 13px;
}

.mission {
    border-radius: 16px;
    padding: 22px;
    text-align: center;
    color: white;
    font-size: 22px;
    font-weight: 900;
    margin: 15px 0;
}

.mission.normal {
    background: linear-gradient(135deg, #16a34a, #22c55e);
}

.mission.alarm {
    background: linear-gradient(135deg, #dc2626, #f97316);
    animation: pulse 0.8s infinite;
}

@keyframes pulse {
    0% { opacity: 1; }
    50% { opacity: 0.68; }
    100% { opacity: 1; }
}

.mini-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
}

.mini-grid div {
    background: #f8fafc;
    border-radius: 14px;
    padding: 14px;
    border: 1px solid #e2e8f0;
}

.mini-grid span {
    display: block;
    color: #64748b;
    font-size: 12px;
    font-weight: 800;
}

.mini-grid strong {
    display: block;
    margin-top: 6px;
    font-size: 22px;
}

.donut-wrap {
    display: flex;
    align-items: center;
    gap: 20px;
}

.donut {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    border: 14px solid #14b8a6;
    border-left-color: #22c55e;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    flex-shrink: 0;
}

.donut span {
    font-size: 30px;
    font-weight: 900;
}

.donut small {
    color: #64748b;
}

.drone-status p {
    margin: 8px 0;
    font-weight: 700;
}

.green-dot,
.cyan-dot,
.orange-dot {
    display: inline-block;
    width: 11px;
    height: 11px;
    border-radius: 999px;
    margin-right: 8px;
}

.green-dot { background: #22c55e; }
.cyan-dot { background: #06b6d4; }
.orange-dot { background: #f59e0b; }

.chart-box {
    width: 100%;
    height: 330px;
    background: white;
    border-radius: 14px;
    border: 1px solid #e2e8f0;
    overflow: hidden;
}

#movementChart {
    width: 100%;
    height: 100%;
}

.thermal-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
    margin-bottom: 14px;
}

.thermal-stats div {
    background: #f8fafc;
    border-radius: 14px;
    border: 1px solid #e2e8f0;
    padding: 14px;
}

.thermal-stats span {
    color: #64748b;
    font-weight: 800;
}

.thermal-stats strong {
    display: block;
    font-size: 24px;
    margin-top: 6px;
}

.thermal-grid {
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    gap: 5px;
}

.thermal-cell {
    height: 36px;
    border-radius: 8px;
    font-size: 11px;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
}

.logs-list {
    max-height: 265px;
    overflow-y: auto;
}

.logs-list div {
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 10px;
    margin-bottom: 8px;
    font-size: 13px;
}

#map {
    height: 360px;
    border-radius: 16px;
    overflow: hidden;
    background: #cbd5e1;
}

.legend {
    display: flex;
    gap: 18px;
    margin-top: 14px;
    flex-wrap: wrap;
    color: #475569;
    font-size: 13px;
    font-weight: 800;
}

.line {
    display: inline-block;
    width: 34px;
    height: 4px;
    border-radius: 999px;
    margin-right: 6px;
}

.cyan { background: #06b6d4; }

.dot {
    display: inline-block;
    width: 12px;
    height: 12px;
    border-radius: 999px;
    margin-right: 6px;
}

.red { background: #ef4444; }
.green { background: #22c55e; }
.orange { background: #f59e0b; }

.cube-panel {
    min-height: 360px;
}

.cube-badge {
    padding: 8px 12px;
    border-radius: 999px;
    font-size: 12px;
    font-weight: 900;
    white-space: nowrap;
}

.cube-badge.online {
    background: #dcfce7;
    color: #166534;
}

.cube-badge.offline {
    background: #fee2e2;
    color: #991b1b;
}

.cube-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 11px;
}

.cube-item {
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    padding: 13px;
}

.cube-item span,
.attitude-row span {
    display: block;
    color: #64748b;
    font-size: 12px;
    font-weight: 800;
}

.cube-item strong {
    display: block;
    margin-top: 6px;
    font-size: 18px;
}

.attitude-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 11px;
    margin-top: 12px;
}

.attitude-row div {
    background: #eff6ff;
    border: 1px solid #bfdbfe;
    border-radius: 14px;
    padding: 13px;
}

.attitude-row strong {
    display: block;
    margin-top: 6px;
    color: #0284c7;
    font-size: 19px;
}

@media (max-width: 1200px) {
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .content-grid,
    .lower-grid {
        grid-template-columns: 1fr;
    }

    .sidebar {
        display: none;
    }
}

@media (max-width: 700px) {
    .main {
        padding: 16px;
    }

    .stats-grid,
    .mini-grid,
    .thermal-stats,
    .cube-grid,
    .attitude-row {
        grid-template-columns: 1fr;
    }

    .topbar {
        flex-direction: column;
        align-items: flex-start;
    }
}
/* ============================= */
/* DETAS FINAL TASARIM DÃœZELTME */
/* ============================= */

body {
    font-family: "Segoe UI", "Inter", "Noto Sans", Arial, sans-serif;
}

/* Kamera ve saÄŸ panel aynÄ± yÃ¼kseklik hissi */
.content-grid {
    align-items: stretch;
}

.camera-panel {
    min-height: 760px;
    display: flex;
    flex-direction: column;
}

.camera-panel #cameraFeed {
    flex: 1;
    min-height: 520px;
    height: 100%;
    object-fit: cover;
}

.right-column {
    display: flex;
    flex-direction: column;
    gap: 18px;
    min-height: 760px;
    height: 100%;
}

.right-column .panel {
    display: flex;
    flex-direction: column;
}

/* GÃ¶rev Durumu biraz uzasÄ±n */
.right-column .panel:first-child {
    flex: 1;
}

/* AI Analiz daha fazla alan kaplasÄ±n */
.right-column .panel:last-child {
    flex: 1.8;
}

.right-column .panel .panel-header {
    flex-shrink: 0;
}

.donut-wrap {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 24px;
}

/* Log alanÄ± TÃ¼rkÃ§e ve satÄ±r taÅŸmasÄ± */
.logs-list {
    max-height: 290px;
    overflow-y: auto;
    white-space: pre-wrap;
    word-break: break-word;
}

.logs-list div {
    line-height: 1.35;
}

/* Harita paneli */
#map {
    height: 360px;
    border-radius: 16px;
    overflow: hidden;
    background: #cbd5e1;
    border: 1px solid #1e3a5f;
}

/* Harita legend daha temiz */
.legend {
    display: flex;
    gap: 18px;
    margin-top: 14px;
    flex-wrap: wrap;
    color: #cbd5e1;
    font-size: 13px;
    font-weight: 800;
}

.legend span {
    display: inline-flex;
    align-items: center;
}

/* Antalya Ã¶zel marker */
.custom-antalya-marker {
    width: 20px;
    height: 20px;
    background: #0ea5e9;
    border: 3px solid #ffffff;
    border-radius: 50%;
    box-shadow: 0 0 0 7px rgba(14, 165, 233, 0.25);
}

/* Risk Ã§emberi daha soft */
.leaflet-popup-content-wrapper {
    border-radius: 14px;
}

/* Dar ekranda bozulmasÄ±n */
@media (max-width: 1200px) {
    .camera-panel,
    .right-column {
        min-height: auto;
    }

    .camera-panel #cameraFeed {
        min-height: 360px;
    }
}
/* Harita etiketleri */
.map-label {
    background: rgba(10, 18, 32, 0.92) !important;
    color: #fff !important;
    border: 1px solid rgba(255,255,255,0.12) !important;
    border-radius: 10px !important;
    padding: 6px 10px !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    box-shadow: 0 6px 18px rgba(0,0,0,0.28) !important;
}

.route-label {
    color: #67e8f9 !important;
}

.danger-label {
    color: #f87171 !important;
}

.medium-label {
    color: #fbbf24 !important;
}

.safe-label {
    color: #4ade80 !important;
}

.leaflet-tooltip.map-label:before {
    display: none !important;
}



/* ============================= */
/* PAN-TILT SERVO PANEL */
/* ============================= */

.servo-panel {
    min-height: 300px;
}

.servo-control {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.servo-row {
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 16px;
    padding: 16px;
}

.servo-row > div:first-child {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 14px;
    gap: 12px;
}

.servo-row strong {
    font-size: 15px;
    color: #0f172a;
}

.servo-row span {
    background: #0f172a;
    color: white;
    border-radius: 999px;
    padding: 6px 12px;
    font-weight: 900;
    font-size: 13px;
}

.servo-row input[type="range"] {
    width: 100%;
    accent-color: #0ea5e9;
}

.servo-buttons {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
}

.servo-buttons button,
.servo-mini-buttons button {
    border: none;
    border-radius: 14px;
    padding: 12px 14px;
    font-weight: 900;
    cursor: pointer;
    color: white;
    background: linear-gradient(90deg, #14b8a6, #0ea5e9);
}

.servo-buttons button:last-child {
    background: linear-gradient(90deg, #f59e0b, #ef4444);
}

.servo-mini-buttons {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
    margin-top: 12px;
}

.servo-mini-buttons button {
    padding: 9px 10px;
    font-size: 12px;
    background: #0f172a;
}

.servo-buttons button:hover,
.servo-mini-buttons button:hover {
    filter: brightness(0.95);
}

@media (max-width: 700px) {
    .servo-buttons,
    .servo-mini-buttons {
        grid-template-columns: 1fr;
    }
}



/* ============================= */
/* SERVO TARAMA MODLARI TASARIM */
/* ============================= */

.servo-mode-title {
    margin-top: 4px;
    margin-bottom: -4px;
    color: #64748b;
    font-size: 13px;
    font-weight: 900;
}

.scan-mode-buttons {
    grid-template-columns: repeat(2, 1fr);
}

.scan-mode-buttons button:nth-child(1) {
    background: linear-gradient(90deg, #0f172a, #334155);
}

.scan-mode-buttons button:nth-child(2) {
    background: linear-gradient(90deg, #14b8a6, #0ea5e9);
}

.scan-mode-buttons button:nth-child(3) {
    background: linear-gradient(90deg, #6366f1, #8b5cf6);
}

.scan-mode-buttons button:nth-child(4) {
    background: linear-gradient(90deg, #f59e0b, #ef4444);
}

@media (max-width: 700px) {
    .scan-mode-buttons {
        grid-template-columns: 1fr;
    }
}



/* ============================= */
/* KAMERA SABÄ°T BOYUT + ZOOM */
/* ============================= */

/* Kamera paneli servo hareketinde boy deÄŸiÅŸtirmesin */
.camera-panel {
    display: flex !important;
    flex-direction: column !important;
    min-height: 760px !important;
    height: 760px !important;
    overflow: hidden !important;
}

/* Kamera gÃ¶rÃ¼ntÃ¼sÃ¼ sabit kutuda kalsÄ±n */
#cameraFeed {
    width: 100% !important;
    height: 620px !important;
    min-height: 620px !important;
    max-height: 620px !important;

    object-fit: cover !important;
    object-position: center center !important;

    display: block !important;
    border-radius: 16px !important;
    border: 1px solid #1e3a5f !important;
    background: #020617 !important;

    transition: transform 0.25s ease, object-position 0.25s ease !important;
    transform-origin: center center !important;
}

/* Zoom aktif olunca gÃ¶rÃ¼ntÃ¼ bÃ¼yÃ¼r ama kutu bÃ¼yÃ¼mez */
#cameraFeed.camera-zoomed {
    transform: scale(1.55) !important;
}

/* Kamera panelinin dÄ±ÅŸÄ±na taÅŸan zoom gÃ¶rÃ¼nmesin */
.camera-panel {
    position: relative !important;
}

.camera-panel #cameraFeed {
    overflow: hidden !important;
}

/* Detections list kamera boyutunu deÄŸiÅŸtirmesin */
.detections-list {
    flex-shrink: 0 !important;
    max-height: 120px !important;
    overflow-y: auto !important;
    margin-top: 14px !important;
}

/* Zoom butonu */
.zoom-btn {
    background: #334155 !important;
}

.zoom-btn.active {
    background: linear-gradient(90deg, #f59e0b, #ef4444) !important;
    color: white !important;
}

/* Kamera butonlarÄ± taÅŸmasÄ±n */
.camera-buttons {
    display: flex !important;
    flex-wrap: wrap !important;
    gap: 8px !important;
}

/* Mobilde daha kÃ¼Ã§Ã¼k sabit yÃ¼kseklik */
@media (max-width: 1200px) {
    .camera-panel {
        height: auto !important;
        min-height: auto !important;
    }

    #cameraFeed {
        height: 420px !important;
        min-height: 420px !important;
        max-height: 420px !important;
    }
}

@media (max-width: 700px) {
    #cameraFeed {
        height: 300px !important;
        min-height: 300px !important;
        max-height: 300px !important;
    }
}



/* ============================= */
/* KAMERA VIEWPORT SABÄ°T + KADEMELÄ° ZOOM */
/* ============================= */

/* Panelin kendisi zoomdan etkilenmez */
.camera-panel {
    overflow: visible !important;
}

/* Sadece gÃ¶rÃ¼ntÃ¼ bu kutunun iÃ§inde bÃ¼yÃ¼r/kÃ¼Ã§Ã¼lÃ¼r */
.camera-viewport {
    width: 100% !important;
    height: 620px !important;
    min-height: 620px !important;
    max-height: 620px !important;
    overflow: hidden !important;

    border-radius: 16px !important;
    border: 1px solid #1e3a5f !important;
    background: #020617 !important;
    position: relative !important;
}

/* Zoom sadece img Ã¼zerinde Ã§alÄ±ÅŸÄ±r */
.camera-viewport #cameraFeed,
#cameraFeed {
    width: 100% !important;
    height: 100% !important;
    min-height: 100% !important;
    max-height: 100% !important;

    object-fit: cover !important;
    object-position: center center !important;

    display: block !important;
    border: none !important;
    border-radius: 0 !important;

    transform: scale(1);
    transform-origin: center center !important;
    transition: transform 0.22s ease !important;
}

/* Butonlar her zaman Ã¼stte ve tÄ±klanabilir kalsÄ±n */
.panel-header,
.camera-buttons,
.camera-btn {
    position: relative !important;
    z-index: 20 !important;
}

.zoom-btn {
    background: #334155 !important;
    color: white !important;
}

#zoomResetBtn {
    min-width: 110px;
}

/* Eski zoom class kalmÄ±ÅŸsa taÅŸma yapmasÄ±n */
#cameraFeed.camera-zoomed {
    transform: scale(1) !important;
}

/* Detections list kamera yÃ¼ksekliÄŸini deÄŸiÅŸtirmesin */
.detections-list {
    flex-shrink: 0 !important;
    max-height: 120px !important;
    overflow-y: auto !important;
    margin-top: 14px !important;
}

/* Servo stop butonu */
.servo-stop-btn {
    background: linear-gradient(90deg, #991b1b, #ef4444) !important;
}

/* Tarama butonlarÄ± 5 butona gÃ¶re dÃ¼zenlensin */
.scan-mode-buttons {
    grid-template-columns: repeat(2, 1fr) !important;
}

.scan-mode-buttons .servo-stop-btn {
    grid-column: span 1;
}

@media (max-width: 1200px) {
    .camera-viewport {
        height: 420px !important;
        min-height: 420px !important;
        max-height: 420px !important;
    }
}

@media (max-width: 700px) {
    .camera-viewport {
        height: 300px !important;
        min-height: 300px !important;
        max-height: 300px !important;
    }

    .scan-mode-buttons {
        grid-template-columns: 1fr !important;
    }
}



/* ===== PAN-TILT taÅŸma dÃ¼zeltme + kamera zoom sÄ±nÄ±rÄ± ===== */
.scan-mode-buttons{
    display:grid !important;
    grid-template-columns:repeat(2, minmax(0,1fr)) !important;
    gap:14px !important;
    width:100% !important;
    margin-top:14px !important;
    align-items:stretch !important;
}

.scan-mode-buttons button{
    width:100% !important;
    min-width:0 !important;
    box-sizing:border-box !important;
    white-space:normal !important;
    word-break:break-word !important;
}

.scan-mode-buttons button:last-child{
    grid-column:1 / -1 !important;
}

.pan-tilt-card,
.servo-card,
.camera-servo-panel{
    overflow:hidden !important;
    padding-bottom:18px !important;
}

.camera-viewport{
    overflow:hidden !important;
    position:relative !important;
    width:100% !important;
    height:620px !important;
    border-radius:18px !important;
}

#cameraFeed{
    width:100% !important;
    height:100% !important;
    object-fit:cover !important;
    object-position:center center !important;
    transform-origin:center center !important;
    transition:transform .20s ease !important;
    will-change:transform !important;
    display:block !important;
}

@media (max-width: 768px){
    .scan-mode-buttons{
        grid-template-columns:1fr !important;
    }

    .scan-mode-buttons button:last-child{
        grid-column:auto !important;
    }

    .camera-viewport{
        height:360px !important;
    }
}



/* ======================================================
   DETAS FINAL LAYOUT FIX
   Tarama modlarÄ± kamera kartÄ±nda, zoom sadece gÃ¶rÃ¼ntÃ¼de
   ====================================================== */

/* Genel yerleÅŸim Ã§akÄ±ÅŸmasÄ±n */
.content-grid {
    align-items: start !important;
    overflow: visible !important;
}

.right-column {
    height: auto !important;
    min-height: auto !important;
    overflow: visible !important;
}

.right-column .panel {
    flex: initial !important;
    height: auto !important;
    min-height: auto !important;
}

.lower-grid,
.bottom-dashboard-grid {
    position: relative !important;
    z-index: 1 !important;
    margin-top: 18px !important;
    clear: both !important;
}

/* Kamera paneli artÄ±k sabit 760px'e zorlanmasÄ±n */
.camera-panel {
    height: auto !important;
    min-height: auto !important;
    overflow: visible !important;
    display: flex !important;
    flex-direction: column !important;
}

/* Kamera gÃ¶rÃ¼ntÃ¼sÃ¼nÃ¼ ayrÄ± kutuya kilitle */
.camera-viewport {
    width: 100% !important;
    height: 560px !important;
    min-height: 560px !important;
    max-height: 560px !important;
    overflow: hidden !important;
    position: relative !important;

    border-radius: 16px !important;
    border: 1px solid #1e3a5f !important;
    background: #020617 !important;
}

/* Zoom sadece img iÃ§inde Ã§alÄ±ÅŸsÄ±n */
.camera-viewport #cameraFeed,
#cameraFeed {
    width: 100% !important;
    height: 100% !important;
    min-height: 100% !important;
    max-height: 100% !important;

    object-fit: cover !important;
    object-position: center center !important;

    display: block !important;
    border: none !important;
    border-radius: 0 !important;

    transform-origin: center center !important;
    transition: transform 0.22s ease !important;
    will-change: transform !important;
}

/* Eski zoom class taÅŸma yapmasÄ±n */
#cameraFeed.camera-zoomed {
    transform: none !important;
}

/* Kamera butonlarÄ± her zaman tÄ±klanabilir */
.panel-header,
.camera-buttons,
.camera-btn {
    position: relative !important;
    z-index: 30 !important;
}

.camera-buttons {
    display: flex !important;
    flex-wrap: wrap !important;
    gap: 8px !important;
}

.zoom-btn {
    background: #334155 !important;
    color: white !important;
}

#zoomResetBtn {
    min-width: 116px !important;
}

/* Detection list boyutu sabit kalsÄ±n */
.detections-list,
.detection-list {
    flex-shrink: 0 !important;
    max-height: 130px !important;
    overflow-y: auto !important;
    margin-top: 14px !important;
}

/* Tarama modlarÄ± artÄ±k kamera kartÄ±nÄ±n altÄ±nda */
.camera-scan-panel {
    margin-top: 16px !important;
    background: rgba(15, 23, 42, 0.65) !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    border-radius: 18px !important;
    padding: 16px !important;
    width: 100% !important;
    box-sizing: border-box !important;
    overflow: hidden !important;
}

.camera-scan-header {
    display: flex !important;
    align-items: flex-start !important;
    justify-content: space-between !important;
    gap: 12px !important;
    margin-bottom: 14px !important;
}

.camera-scan-header h2 {
    margin: 0 !important;
    font-size: 19px !important;
    color: #f8fafc !important;
    font-weight: 900 !important;
}

.camera-scan-header p {
    margin: 5px 0 0 !important;
    color: #c7bda9 !important;
    font-size: 13px !important;
}

.camera-scan-header span {
    background: #052e16 !important;
    color: #86efac !important;
    border-radius: 999px !important;
    padding: 7px 12px !important;
    font-weight: 900 !important;
    font-size: 12px !important;
}

/* Tarama butonlarÄ± taÅŸmasÄ±n */
.camera-scan-panel .scan-mode-buttons {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 12px !important;
    width: 100% !important;
    margin: 0 !important;
}

.camera-scan-panel .scan-mode-buttons button,
.scan-fixed-btn {
    width: 100% !important;
    min-width: 0 !important;
    box-sizing: border-box !important;
    border: none !important;
    border-radius: 14px !important;
    padding: 14px 12px !important;
    color: white !important;
    font-size: 15px !important;
    font-weight: 900 !important;
    cursor: pointer !important;
    white-space: normal !important;
    word-break: break-word !important;
}

.camera-scan-panel .scan-mode-buttons button:last-child,
.full-scan-btn {
    grid-column: 1 / -1 !important;
}

/* Buton renkleri */
.center-scan-btn {
    background: linear-gradient(90deg, #0f172a, #334155) !important;
}

.servo-stop-btn {
    background: linear-gradient(90deg, #991b1b, #ef4444) !important;
}

.pan-scan-btn {
    background: linear-gradient(90deg, #1d4ed8, #7c3aed) !important;
}

.tilt-scan-btn {
    background: linear-gradient(90deg, #d97706, #dc2626) !important;
}

.full-scan-btn {
    background: linear-gradient(90deg, #f59e0b, #b91c1c) !important;
}

/* Servo panel iÃ§inde tarama kÄ±smÄ± kalÄ±rsa gizle */
.servo-panel > .servo-mode-title,
.servo-panel > .scan-mode-buttons {
    display: none !important;
}

/* SaÄŸdaki servo panel taÅŸmasÄ±n */
.servo-panel {
    height: auto !important;
    min-height: auto !important;
    overflow: visible !important;
    padding-bottom: 18px !important;
}

/* Alt kartlarÄ±n Ã¼st Ã¼ste binmesini engelle */
.log-panel,
.logs-card {
    position: relative !important;
    z-index: 1 !important;
}

/* Mobil */
@media (max-width: 1200px) {
    .camera-viewport {
        height: 420px !important;
        min-height: 420px !important;
        max-height: 420px !important;
    }
}

@media (max-width: 700px) {
    .camera-viewport {
        height: 300px !important;
        min-height: 300px !important;
        max-height: 300px !important;
    }

    .camera-scan-panel .scan-mode-buttons {
        grid-template-columns: 1fr !important;
    }

    .camera-scan-panel .scan-mode-buttons button:last-child,
    .full-scan-btn {
        grid-column: auto !important;
    }
}



/* =========================================================
   DETAS CAMERA FIXED PANEL + SEPARATE SCAN CARD + LIMIT LOGS
   ========================================================= */

/* Kamera ana kutusu sabit kalsÄ±n */
.camera-panel {
    display: flex !important;
    flex-direction: column !important;
    gap: 14px !important;
    overflow: hidden !important;
    min-height: 0 !important;
    box-sizing: border-box !important;
}

/* Kamera gÃ¶rÃ¼ntÃ¼sÃ¼ sabit yÃ¼kseklik */
.camera-viewport {
    width: 100% !important;
    height: 560px !important;
    min-height: 560px !important;
    max-height: 560px !important;
    overflow: hidden !important;
    position: relative !important;
    border-radius: 16px !important;
    border: 1px solid #21426a !important;
    background: #020617 !important;
    flex-shrink: 0 !important;
}

/* GÃ¶rÃ¼ntÃ¼ sadece bu alanÄ±n iÃ§inde kalsÄ±n */
.camera-viewport #cameraFeed,
#cameraFeed {
    width: 100% !important;
    height: 100% !important;
    min-height: 100% !important;
    max-height: 100% !important;
    object-fit: cover !important;
    object-position: center center !important;
    display: block !important;
    border-radius: 0 !important;
    border: none !important;
    transform-origin: center center !important;
    will-change: transform !important;
}

/* AlgÄ±lama kayÄ±t alanÄ± */
.camera-detections-wrap {
    width: 100% !important;
    height: 150px !important;
    min-height: 150px !important;
    max-height: 150px !important;
    overflow: hidden !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    border-radius: 14px !important;
    background: rgba(2, 6, 23, 0.55) !important;
    padding: 10px 12px !important;
    box-sizing: border-box !important;
    flex-shrink: 0 !important;
}

.camera-detections-title {
    font-size: 13px !important;
    font-weight: 800 !important;
    color: #cbd5e1 !important;
    margin-bottom: 8px !important;
}

.camera-detections-list,
.detections-list,
.detection-list {
    height: calc(100% - 24px) !important;
    min-height: calc(100% - 24px) !important;
    max-height: calc(100% - 24px) !important;
    overflow-y: auto !important;
    overflow-x: hidden !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 8px !important;
    padding-right: 4px !important;
    box-sizing: border-box !important;
}

/* En fazla 5 eleman gÃ¶rÃ¼nsÃ¼n, fazlasÄ± gizlensin */
.camera-detections-list > *:nth-child(n+6),
.detections-list > *:nth-child(n+6),
.detection-list > *:nth-child(n+6) {
    display: none !important;
}

/* Tarama modlarÄ± ayrÄ± kutu olsun */
.detached-scan-card,
.camera-scan-panel.detached-scan-card {
    margin-top: 18px !important;
    width: 100% !important;
    box-sizing: border-box !important;
    border-radius: 18px !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    background: rgba(15, 23, 42, 0.72) !important;
    padding: 16px !important;
    overflow: hidden !important;
    position: relative !important;
    z-index: 1 !important;
}

/* Kamera panelinin iÃ§ine gÃ¶mÃ¼lÃ¼ gibi gÃ¶rÃ¼nmesin */
.camera-panel .camera-scan-panel {
    margin-top: 0 !important;
}

/* Tarama buton alanÄ± */
.detached-scan-card .scan-mode-buttons,
.camera-scan-panel.detached-scan-card .scan-mode-buttons {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 12px !important;
    width: 100% !important;
}

.detached-scan-card .scan-mode-buttons button,
.camera-scan-panel.detached-scan-card .scan-mode-buttons button {
    width: 100% !important;
    min-width: 0 !important;
    box-sizing: border-box !important;
    white-space: normal !important;
    word-break: break-word !important;
}

.detached-scan-card .scan-mode-buttons button:last-child,
.camera-scan-panel.detached-scan-card .scan-mode-buttons button:last-child {
    grid-column: 1 / -1 !important;
}

/* Mobil uyum */
@media (max-width: 1200px) {
    .camera-viewport {
        height: 420px !important;
        min-height: 420px !important;
        max-height: 420px !important;
    }
}

@media (max-width: 700px) {
    .camera-viewport {
        height: 300px !important;
        min-height: 300px !important;
        max-height: 300px !important;
    }

    .camera-detections-wrap {
        height: 140px !important;
        min-height: 140px !important;
        max-height: 140px !important;
    }

    .detached-scan-card .scan-mode-buttons,
    .camera-scan-panel.detached-scan-card .scan-mode-buttons {
        grid-template-columns: 1fr !important;
    }

    .detached-scan-card .scan-mode-buttons button:last-child,
    .camera-scan-panel.detached-scan-card .scan-mode-buttons button:last-child {
        grid-column: auto !important;
    }
}

/* Kamera kartÄ± kendi normal gÃ¶rÃ¼nÃ¼mÃ¼nde kalsÄ±n */
.detas-restored-camera-card {
    width: 100% !important;
    min-width: 0 !important;
    max-width: 100% !important;
    box-sizing: border-box !important;
}

/* Tarama ModlarÄ± artÄ±k saÄŸda deÄŸil, kameranÄ±n altÄ±nda ayrÄ± kart */
.detas-restored-scan-card {
    width: 100% !important;
    min-width: 0 !important;
    max-width: 100% !important;
    box-sizing: border-box !important;

    background: rgba(15, 23, 42, 0.72) !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    border-radius: 18px !important;
    padding: 18px !important;
    box-shadow: none !important;

    display: block !important;
    position: relative !important;
    overflow: hidden !important;
}

/* BaÅŸlÄ±k/metin renkleri diÄŸer kartlarla uyumlu */
.detas-restored-scan-card h1,
.detas-restored-scan-card h2,
.detas-restored-scan-card h3,
.detas-restored-scan-card h4,
.detas-restored-scan-card h5,
.detas-restored-scan-card h6 {
    color: #f8fafc !important;
}

.detas-restored-scan-card p,
.detas-restored-scan-card span,
.detas-restored-scan-card small,
.detas-restored-scan-card label {
    color: #cbd5e1 !important;
}

/* Aktif rozeti gÃ¶rÃ¼nÃ¼mÃ¼ */
.detas-restored-scan-card .badge,
.detas-restored-scan-card .status-badge,
.detas-restored-scan-card .pill,
.detas-restored-scan-card .active-badge {
    background: rgba(34, 197, 94, 0.18) !important;
    color: #4ade80 !important;
    border: 1px solid rgba(34, 197, 94, 0.35) !important;
    border-radius: 999px !important;
}

/* Buton dÃ¼zeni */
.detas-restored-scan-card .scan-mode-buttons,
.detas-restored-scan-card .tarama-buttons,
.detas-restored-scan-card .tarama-modlari-buttons,
.detas-restored-scan-card .button-grid {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 12px !important;
    width: 100% !important;
}

.detas-restored-scan-card button {
    width: 100% !important;
    min-width: 0 !important;
    max-width: 100% !important;
    box-sizing: border-box !important;
    white-space: normal !important;
    word-break: break-word !important;
    border-radius: 14px !important;
}

/* Tam tarama tam satÄ±r */
.detas-restored-scan-card .scan-mode-buttons button:last-child,
.detas-restored-scan-card .tarama-buttons button:last-child,
.detas-restored-scan-card .tarama-modlari-buttons button:last-child,
.detas-restored-scan-card .button-grid button:last-child {
    grid-column: 1 / -1 !important;
}

/* Mobilde tek sÃ¼tun */
@media (max-width: 768px) {
    .detas-restored-scan-card .scan-mode-buttons,
    .detas-restored-scan-card .tarama-buttons,
    .detas-restored-scan-card .tarama-modlari-buttons,
    .detas-restored-scan-card .button-grid {
        grid-template-columns: 1fr !important;
    }

    .detas-restored-scan-card .scan-mode-buttons button:last-child,
    .detas-restored-scan-card .tarama-buttons button:last-child,
    .detas-restored-scan-card .tarama-modlari-buttons button:last-child,
    .detas-restored-scan-card .button-grid button:last-child {
        grid-column: auto !important;
    }
}

/* GENEL */
html, body {
    margin: 0;
    padding: 0;
    min-height: 100%;
    width: 100%;
    background: #06122b;
    overflow-x: hidden;
}

body {
    position: relative;
    color: #ffffff;
    font-family: Arial, Helvetica, sans-serif;
}

/* TÃœM SAYFAYI KAPLAYAN ANA ARKA PLAN */
body::before {
    content: "";
    position: fixed;
    inset: 0;
    background:
        linear-gradient(180deg, #081631 0%, #07152f 45%, #061328 100%);
    z-index: -30;
}

/* SIDEBAR SABÄ°T VE TAM YÃœKSEKLÄ°K */
.sidebar,
#sidebar,
.left-sidebar {
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    width: 155px !important;
    height: 100vh !important;
    min-height: 100vh !important;
    background: linear-gradient(180deg, #041129 0%, #031022 100%) !important;
    border-right: 1px solid rgba(0, 180, 255, 0.18) !important;
    box-shadow: 6px 0 24px rgba(0,0,0,0.28) !important;
    z-index: 20 !important;
    display: flex !important;
    flex-direction: column !important;
    overflow: hidden !important;
}

/* SIDEBAR Ä°Ã‡Ä° */
.sidebar .logo,
#sidebar .logo,
.left-sidebar .logo {
    flex: 0 0 auto;
}

.sidebar nav,
#sidebar nav,
.left-sidebar nav {
    flex: 1 1 auto;
    overflow-y: auto;
    padding-bottom: 14px;
}

.sidebar .sidebar-footer,
#sidebar .sidebar-footer,
.left-sidebar .sidebar-footer,
.sidebar .system-status,
#sidebar .system-status,
.left-sidebar .system-status {
    margin-top: auto;
    padding-bottom: 14px;
}

/* ANA Ä°Ã‡ERÄ°K - SIDEBARIN SAÄINA OTURSUN */
.main-content,
.content,
.dashboard-content,
.app-content,
.page-content,
.container-right {
    position: relative !important;
    z-index: 5 !important;
    margin-left: 155px !important;
    width: calc(100% - 155px) !important;
    min-height: 100vh !important;
    padding: 16px 18px 24px 18px !important;
    box-sizing: border-box !important;
    background: transparent !important;
}

/* HEADER / TOP BAR */
.topbar,
.header-bar,
.dashboard-header,
.page-header {
    position: relative !important;
    z-index: 6 !important;
    width: 100% !important;
    background: transparent !important;
}

/* EN ÃœSTTEKÄ° 4 KART */
.top-cards,
.summary-cards,
.header-cards,
.overview-cards {
    display: grid !important;
    grid-template-columns: repeat(4, minmax(0, 1fr)) !important;
    gap: 14px !important;
    width: 100% !important;
    margin-bottom: 14px !important;
}

/* ANA GRID */
.main-grid,
.dashboard-grid,
.content-grid,
.layout-grid {
    display: grid !important;
    grid-template-columns: minmax(0, 2fr) minmax(340px, 0.9fr) !important;
    gap: 14px !important;
    align-items: start !important;
    width: 100% !important;
}

/* KARTLAR */
.card,
.panel,
.widget,
.info-box,
.stat-box,
.section-box {
    position: relative !important;
    z-index: 6 !important;
    background: rgba(9, 18, 30, 0.92) !important;
    border: 1px solid rgba(0, 170, 255, 0.22) !important;
    border-radius: 16px !important;
    box-shadow: 0 6px 20px rgba(0,0,0,0.18) !important;
    box-sizing: border-box !important;
    overflow: hidden !important;
}

/* KAMERA ALANI */
.camera-panel,
#camera-panel,
.live-camera-box {
    display: flex !important;
    flex-direction: column !important;
    gap: 10px !important;
}

.camera-toolbar,
#camera-toolbar,
.camera-controls-top {
    display: flex !important;
    flex-wrap: nowrap !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: 8px !important;
    width: 100% !important;
}

/* ZOOM + KAMERA TUÅLARI KÃœÃ‡ÃœK VE YAN YANA */
.camera-toolbar button,
#camera-toolbar button,
.camera-controls-top button,
.camera-switch-btn,
.zoom-btn {
    min-width: 74px !important;
    height: 30px !important;
    padding: 0 10px !important;
    border-radius: 9px !important;
    font-size: 12px !important;
    font-weight: 700 !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    white-space: nowrap !important;
}

/* KAMERA FRAME */
.camera-frame,
#camera-frame,
.camera-view,
.video-frame {
    width: 100% !important;
    aspect-ratio: 16 / 9 !important;
    min-height: 420px !important;
    max-height: 560px !important;
    border: 1px solid rgba(0, 170, 255, 0.25) !important;
    border-radius: 16px !important;
    overflow: hidden !important;
    background: #050b14 !important;
    position: relative !important;
}

/* KAMERA GÃ–RÃœNTÃœSÃœ */
.camera-frame img,
#camera-frame img,
.camera-view img,
.video-frame img,
.camera-frame canvas,
#camera-frame canvas,
.camera-view canvas {
    width: 100% !important;
    height: 100% !important;
    object-fit: contain !important;
    display: block !important;
    background: #050b14 !important;
}

/* ALGILAMA KAYITLARI */
.detection-log,
#detection-log,
.algilama-kayitlari {
    min-height: 88px !important;
    max-height: 120px !important;
    overflow-y: auto !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 8px !important;
}

/* TARAMA MODLARI */
.scan-panel,
#scan-panel,
.tarama-panel {
    margin-top: 14px !important;
}

.scan-grid,
#scan-grid,
.tarama-grid {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 10px !important;
}

.scan-grid .full,
#scan-grid .full,
.tarama-grid .full {
    grid-column: 1 / -1 !important;
}

.scan-grid button,
#scan-grid button,
.tarama-grid button {
    width: 100% !important;
    min-height: 42px !important;
    border-radius: 12px !important;
    font-weight: 700 !important;
}

/* SAÄ PANEL Ä°Ã‡ GRID */
.right-column,
.right-side,
.side-widgets {
    display: flex !important;
    flex-direction: column !important;
    gap: 14px !important;
    width: 100% !important;
}

/* PAN-TILT */
.pan-tilt-panel,
#pantilt-panel {
    display: flex !important;
    flex-direction: column !important;
    gap: 12px !important;
}

.pan-tilt-panel .row,
#pantilt-panel .row {
    display: grid !important;
    grid-template-columns: 1fr auto !important;
    gap: 10px !important;
    align-items: center !important;
}

.pan-tilt-panel .button-row,
#pantilt-panel .button-row {
    display: grid !important;
    grid-template-columns: repeat(3, 1fr) !important;
    gap: 8px !important;
}

/* ALT GRID */
.bottom-grid,
.analysis-grid,
.lower-grid {
    display: grid !important;
    grid-template-columns: minmax(0, 1.25fr) minmax(0, 1fr) minmax(0, 1fr) !important;
    gap: 14px !important;
    width: 100% !important;
    margin-top: 14px !important;
}

/* HARÄ°TA */
.map-panel,
#map-panel {
    margin-top: 14px !important;
}

/* GENÄ°ÅLÄ°K / RESPONSIVE */
@media (max-width: 1400px) {
    .main-grid,
    .dashboard-grid,
    .content-grid,
    .layout-grid {
        grid-template-columns: 1fr !important;
    }

    .bottom-grid,
    .analysis-grid,
    .lower-grid {
        grid-template-columns: 1fr !important;
    }
}

@media (max-width: 1000px) {
    .top-cards,
    .summary-cards,
    .header-cards,
    .overview-cards {
        grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    }
}

@media (max-width: 768px) {
    .sidebar,
    #sidebar,
    .left-sidebar {
        width: 100px !important;
    }

    .main-content,
    .content,
    .dashboard-content,
    .app-content,
    .page-content,
    .container-right {
        margin-left: 100px !important;
        width: calc(100% - 100px) !important;
        padding: 12px !important;
    }

    .top-cards,
    .summary-cards,
    .header-cards,
    .overview-cards {
        grid-template-columns: 1fr !important;
    }

    .camera-toolbar,
    #camera-toolbar,
    .camera-controls-top {
        flex-wrap: wrap !important;
        justify-content: flex-start !important;
    }

    .scan-grid,
    #scan-grid,
    .tarama-grid {
        grid-template-columns: 1fr !important;
    }

    .pan-tilt-panel .button-row,
    #pantilt-panel .button-row {
        grid-template-columns: 1fr !important;
    }
}

/* Sayfa genel alanÄ± */
html,
body {
    margin: 0 !important;
    padding: 0 !important;
    min-height: 100vh !important;
    width: 100% !important;
    background: #101722 !important;
    overflow-x: hidden !important;
}

/* Body'ye sidebar kadar sol gÃ¼venli alan bÄ±rak */
body {
    padding-left: var(--detas-sidebar-safe-width) !important;
    box-sizing: border-box !important;
}

/* Sol sidebar gerÃ§ek katman olarak sabit kalsÄ±n */
.sidebar,
.side-panel,
.left-sidebar,
.detas-sidebar-fixed,
aside {
    position: fixed !important;
    left: 0 !important;
    top: 0 !important;
    bottom: 0 !important;

    width: var(--detas-sidebar-safe-width) !important;
    min-width: var(--detas-sidebar-safe-width) !important;
    max-width: var(--detas-sidebar-safe-width) !important;

    height: 100vh !important;
    min-height: 100vh !important;

    background: linear-gradient(180deg, #06162a 0%, #020b16 100%) !important;
    z-index: 1000 !important;

    box-sizing: border-box !important;
    overflow: hidden !important;
}

/* Ana iÃ§erikte eski margin-left ayarlarÄ±nÄ± iptal et */
main,
.main-content,
.content-area,
.dashboard-content,
.detas-main-fixed,
#detasAppShell {
    margin-left: 0 !important;
    padding-left: 0 !important;
    left: auto !important;

    width: 100% !important;
    max-width: 100% !important;
    min-width: 0 !important;

    box-sizing: border-box !important;
    position: relative !important;
    z-index: 10 !important;
}

/* Ä°Ã§erik kenarlardan dÃ¼zgÃ¼n dursun */
main,
.main-content,
.content-area,
.dashboard-content,
.detas-main-fixed {
    padding: 16px 18px 24px 18px !important;
}

/* Shell grid bozmasÄ±n */
#detasAppShell {
    display: block !important;
    min-height: 100vh !important;
}

/* KartlarÄ±n yanlÄ±ÅŸlÄ±kla beyaz olmasÄ±nÄ± engelle */
.card,
.panel,
.stat-card,
.info-card,
.servo-panel,
.camera-panel,
.log-panel,
.logs-card,
.thermal-panel,
.map-panel,
.mission-panel {
    background: #15191c !important;
    color: #f8fafc !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
}

/* Ä°Ã§ mini kutular beyaza dÃ¶nmesin */
.card *:not(button):not(input):not(canvas):not(img),
.panel *:not(button):not(input):not(canvas):not(img) {
    color: inherit;
}

/* Beyaza dÃ¶nen kÃ¼Ã§Ã¼k bilgi kutularÄ±nÄ± toparla */
.mini-card,
.mini-box,
.status-box,
.servo-block,
.thermal-box,
.log-line,
.detection-item,
.camera-detections-wrap {
    background: #111827 !important;
    color: #f8fafc !important;
    border: 1px solid rgba(56, 189, 248, 0.18) !important;
}

/* Kamera Ã¼st butonlarÄ± tek sÄ±rada kÃ¼Ã§Ã¼k dursun */
.camera-buttons,
.detas-camera-toolbar {
    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: 8px !important;
    width: 100% !important;
    overflow-x: auto !important;
}

.camera-buttons button,
.detas-camera-toolbar button,
.camera-btn,
.zoom-btn {
    width: auto !important;
    min-width: 72px !important;
    max-width: 130px !important;
    height: 32px !important;
    padding: 6px 10px !important;
    font-size: 12px !important;
    line-height: 1 !important;
    border-radius: 9px !important;
    white-space: nowrap !important;
    flex: 0 0 auto !important;
}

/* Zoom gÃ¶sterge butonu */
#zoomResetBtn {
    min-width: 92px !important;
}

/* Kamera panelinin yÃ¼ksekliÄŸi yazÄ±lara gÃ¶re zÄ±plamasÄ±n */
.camera-viewport {
    height: 560px !important;
    min-height: 560px !important;
    max-height: 560px !important;
    overflow: hidden !important;
}

/* AlgÄ±lama kayÄ±tlarÄ± en fazla sabit alan kaplasÄ±n */
.camera-detections-wrap,
.detections-list,
.detection-list {
    max-height: 130px !important;
    overflow-y: auto !important;
}

/* Tarama modlarÄ± kameranÄ±n altÄ±nda kalsÄ±n */
.camera-scan-panel,
.detached-scan-card,
.detas-restored-scan-card {
    width: 100% !important;
    max-width: 100% !important;
    box-sizing: border-box !important;
    background: #15191c !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    border-radius: 16px !important;
    color: #f8fafc !important;
}

/* Responsive */
@media (max-width: 900px) {
    body {
        padding-left: 0 !important;
    }

    .sidebar,
    .side-panel,
    .left-sidebar,
    .detas-sidebar-fixed,
    aside {
        position: relative !important;
        width: 100% !important;
        min-width: 100% !important;
        max-width: 100% !important;
        height: auto !important;
        min-height: auto !important;
    }

    main,
    .main-content,
    .content-area,
    .dashboard-content,
    .detas-main-fixed {
        padding: 12px !important;
    }
}

/* Body iÃ§erik alanÄ±nÄ± daraltma; eski dÃ¼zeni koru */
html,
body {
    margin: 0 !important;
    padding-left: 0 !important;
    background: #101722 !important;
    color: #f8fafc !important;
    overflow-x: hidden !important;
}

/* Shell varsa normal flex/grid akÄ±ÅŸÄ±nda kalsÄ±n */
#detasAppShell {
    display: flex !important;
    align-items: stretch !important;
    width: 100% !important;
    min-height: 100vh !important;
}

/* Sidebar geniÅŸ ve tam boy */
.sidebar,
.side-panel,
.left-sidebar,
.detas-sidebar-fixed,
aside {
    width: var(--detas-sidebar-final-width) !important;
    min-width: var(--detas-sidebar-final-width) !important;
    max-width: var(--detas-sidebar-final-width) !important;

    min-height: 100vh !important;
    height: 100vh !important;

    position: sticky !important;
    top: 0 !important;
    left: 0 !important;

    align-self: stretch !important;
    flex-shrink: 0 !important;

    background: linear-gradient(180deg, #06162a 0%, #020b16 100%) !important;
    border-right: 1px solid rgba(56, 189, 248, 0.14) !important;
    box-sizing: border-box !important;

    display: flex !important;
    flex-direction: column !important;
    overflow: hidden !important;
    z-index: 20 !important;
}

/* Sidebar alt durum yazÄ±sÄ± aÅŸaÄŸÄ±da kalsÄ±n */
.sidebar > *:last-child,
.side-panel > *:last-child,
.left-sidebar > *:last-child,
.detas-sidebar-fixed > *:last-child,
aside > *:last-child {
    margin-top: auto !important;
}

/* Ana iÃ§erik sidebar'Ä±n yanÄ±nda normal aksÄ±n */
main,
.main-content,
.content-area,
.dashboard-content,
.detas-main-fixed {
    margin-left: 0 !important;
    padding-left: 18px !important;
    width: auto !important;
    max-width: none !important;
    min-width: 0 !important;
    flex: 1 1 auto !important;
    box-sizing: border-box !important;
    background: transparent !important;
}

/* BeyazlaÅŸan kartlarÄ± tekrar koyu tema yap */
.card,
.panel,
.stat-card,
.metric-card,
.summary-card,
.info-card,
.servo-panel,
.camera-panel,
.log-panel,
.logs-card,
.thermal-panel,
.map-panel,
.mission-panel,
div[class*="card"],
div[class*="panel"] {
    background: #15191c !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.22) !important;
}

/* BeyazlaÅŸan iÃ§ kutular */
.mini-card,
.mini-box,
.status-box,
.servo-block,
.thermal-box,
.log-line,
.detection-item,
.camera-detections-wrap,
.detections-list,
.detection-list,
div[class*="box"],
div[class*="item"] {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.18) !important;
}

/* Butonlar ve inputlar tema dÄ±ÅŸÄ±na Ã§Ä±kmasÄ±n */
button {
    color: #ffffff !important;
}

input[type="range"] {
    accent-color: #38bdf8 !important;
}

/* Kamera Ã¼st butonlarÄ± kÃ¼Ã§Ã¼k ve tek sÄ±ra */
.camera-buttons,
.detas-camera-toolbar {
    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: 8px !important;
    width: 100% !important;
    overflow-x: auto !important;
}

.camera-buttons button,
.detas-camera-toolbar button,
.camera-btn,
.zoom-btn {
    width: auto !important;
    min-width: 72px !important;
    max-width: 120px !important;
    height: 32px !important;
    padding: 6px 10px !important;
    font-size: 12px !important;
    line-height: 1 !important;
    border-radius: 9px !important;
    white-space: nowrap !important;
    flex: 0 0 auto !important;
}

#zoomResetBtn {
    min-width: 92px !important;
}

/* Kamera ana kutusu sabit kalsÄ±n */
.camera-viewport {
    height: 560px !important;
    min-height: 560px !important;
    max-height: 560px !important;
    overflow: hidden !important;
}

/* AlgÄ±lama alanÄ± kartÄ± bÃ¼yÃ¼tmesin */
.camera-detections-wrap,
.detections-list,
.detection-list {
    max-height: 130px !important;
    overflow-y: auto !important;
}

/* Tarama kartÄ± diÄŸer kutularla aynÄ± renk */
.camera-scan-panel,
.detached-scan-card,
.detas-restored-scan-card {
    background: #15191c !important;
    color: #f8fafc !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    border-radius: 16px !important;
}

/* Mobilde sidebar normal aksÄ±n */
@media (max-width: 900px) {
    #detasAppShell {
        display: block !important;
    }

    .sidebar,
    .side-panel,
    .left-sidebar,
    .detas-sidebar-fixed,
    aside {
        position: relative !important;
        width: 100% !important;
        min-width: 100% !important;
        max-width: 100% !important;
        height: auto !important;
        min-height: auto !important;
    }

    main,
    .main-content,
    .content-area,
    .dashboard-content,
    .detas-main-fixed {
        padding-left: 12px !important;
        width: 100% !important;
    }
}





/* =========================================================
   DETAS FINAL CONTENT BOX LAYOUT
   Sidebar sabit, dashboard tek ana kutu iÃ§inde
   ========================================================= */

:root {
    --detas-sidebar-width-final: 170px;
    --detas-page-gap-final: 14px;
}

html,
body {
    margin: 0 !important;
    padding: 0 !important;
    width: 100% !important;
    min-height: 100vh !important;
    overflow-x: hidden !important;
    background: #0f172a !important;
    color: #f8fafc !important;
}

/* Sol sidebar gerÃ§ek tam boy katman */
#detasSidebarFinal {
    position: fixed !important;
    left: 0 !important;
    top: 0 !important;
    bottom: 0 !important;

    width: var(--detas-sidebar-width-final) !important;
    min-width: var(--detas-sidebar-width-final) !important;
    max-width: var(--detas-sidebar-width-final) !important;

    height: 100vh !important;
    min-height: 100vh !important;
    max-height: 100vh !important;

    background: linear-gradient(180deg, #06162a 0%, #020b16 100%) !important;
    border-right: 1px solid rgba(56, 189, 248, 0.16) !important;

    z-index: 1000 !important;
    box-sizing: border-box !important;

    display: flex !important;
    flex-direction: column !important;
    overflow: hidden !important;
}

/* Sidebar iÃ§indeki son durum yazÄ±sÄ± alta yaslansÄ±n */
#detasSidebarFinal > *:last-child {
    margin-top: auto !important;
}

/* SaÄŸ taraf ana alan */
#detasMainAreaFinal {
    position: relative !important;
    z-index: 10 !important;

    margin-left: var(--detas-sidebar-width-final) !important;
    width: calc(100% - var(--detas-sidebar-width-final)) !important;
    min-height: 100vh !important;

    padding: var(--detas-page-gap-final) !important;
    box-sizing: border-box !important;

    background: #0f172a !important;
}

/* BÃ¼tÃ¼n dashboard bunun iÃ§ine alÄ±nÄ±r */
#detasContentBoxFinal {
    width: 100% !important;
    min-height: calc(100vh - 28px) !important;

    background: rgba(7, 15, 29, 0.58) !important;
    border: 1px solid rgba(56, 189, 248, 0.10) !important;
    border-radius: 18px !important;

    padding: 14px !important;
    box-sizing: border-box !important;
    position: relative !important;
    z-index: 11 !important;
    overflow: visible !important;
}

/* Eski shell iÃ§eride kalÄ±rsa layout bozmasÄ±n */
#detasContentBoxFinal #detasAppShell,
#detasContentBoxFinal .detas-main-fixed,
#detasContentBoxFinal main,
#detasContentBoxFinal .main-content,
#detasContentBoxFinal .content-area,
#detasContentBoxFinal .dashboard-content {
    display: block !important;
    width: 100% !important;
    max-width: 100% !important;
    min-width: 0 !important;
    margin-left: 0 !important;
    padding-left: 0 !important;
    box-sizing: border-box !important;
    background: transparent !important;
}

/* Kartlar koyu tema */
#detasContentBoxFinal .card,
#detasContentBoxFinal .panel,
#detasContentBoxFinal .stat-card,
#detasContentBoxFinal .metric-card,
#detasContentBoxFinal .summary-card,
#detasContentBoxFinal .info-card,
#detasContentBoxFinal .servo-panel,
#detasContentBoxFinal .camera-panel,
#detasContentBoxFinal .log-panel,
#detasContentBoxFinal .logs-card,
#detasContentBoxFinal .thermal-panel,
#detasContentBoxFinal .map-panel,
#detasContentBoxFinal .mission-panel,
#detasContentBoxFinal div[class*="card"],
#detasContentBoxFinal div[class*="panel"] {
    background: #15191c !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.22) !important;
}

/* BeyazlaÅŸan kÃ¼Ã§Ã¼k kutular */
#detasContentBoxFinal .mini-card,
#detasContentBoxFinal .mini-box,
#detasContentBoxFinal .status-box,
#detasContentBoxFinal .servo-block,
#detasContentBoxFinal .thermal-box,
#detasContentBoxFinal .log-line,
#detasContentBoxFinal .detection-item,
#detasContentBoxFinal .camera-detections-wrap,
#detasContentBoxFinal .detections-list,
#detasContentBoxFinal .detection-list {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.18) !important;
}

/* YazÄ±lar okunabilir olsun */
#detasContentBoxFinal h1,
#detasContentBoxFinal h2,
#detasContentBoxFinal h3,
#detasContentBoxFinal h4,
#detasContentBoxFinal strong,
#detasContentBoxFinal label {
    color: #f8fafc !important;
}

#detasContentBoxFinal p,
#detasContentBoxFinal small,
#detasContentBoxFinal span {
    color: inherit;
}

/* Ãœst kartlar beyaz kalmasÄ±n */
#detasContentBoxFinal [style*="background: white"],
#detasContentBoxFinal [style*="background:white"],
#detasContentBoxFinal [style*="background-color: white"],
#detasContentBoxFinal [style*="background-color:white"] {
    background: #15191c !important;
    color: #f8fafc !important;
}

/* Kamera butonlarÄ± kÃ¼Ã§Ã¼k, yan yana */
#detasContentBoxFinal .camera-buttons,
#detasContentBoxFinal .detas-camera-toolbar {
    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: 8px !important;
    width: 100% !important;
    overflow-x: auto !important;
    overflow-y: hidden !important;
}

#detasContentBoxFinal .camera-buttons button,
#detasContentBoxFinal .detas-camera-toolbar button,
#detasContentBoxFinal .camera-btn,
#detasContentBoxFinal .zoom-btn {
    width: auto !important;
    min-width: 72px !important;
    max-width: 120px !important;
    height: 32px !important;
    padding: 6px 10px !important;
    font-size: 12px !important;
    line-height: 1 !important;
    border-radius: 9px !important;
    white-space: nowrap !important;
    flex: 0 0 auto !important;
}

#detasContentBoxFinal #zoomResetBtn {
    min-width: 92px !important;
}

/* Kamera gÃ¶rÃ¼ntÃ¼ alanÄ± sabit */
#detasContentBoxFinal .camera-viewport {
    height: 560px !important;
    min-height: 560px !important;
    max-height: 560px !important;
    overflow: hidden !important;
}

/* AlgÄ±lama alanÄ± kartÄ± bÃ¼yÃ¼tmesin */
#detasContentBoxFinal .camera-detections-wrap,
#detasContentBoxFinal .detections-list,
#detasContentBoxFinal .detection-list {
    max-height: 130px !important;
    overflow-y: auto !important;
}

/* Tarama kartÄ± */
#detasContentBoxFinal .camera-scan-panel,
#detasContentBoxFinal .detached-scan-card,
#detasContentBoxFinal .detas-restored-scan-card {
    background: #15191c !important;
    color: #f8fafc !important;
    border: 1px solid rgba(56, 189, 248, 0.22) !important;
    border-radius: 16px !important;
}

/* Scrollbar Ã§ok kalÄ±n gÃ¶rÃ¼nmesin */
#detasContentBoxFinal ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
}

#detasContentBoxFinal ::-webkit-scrollbar-thumb {
    background: rgba(148, 163, 184, 0.55);
    border-radius: 999px;
}

#detasContentBoxFinal ::-webkit-scrollbar-track {
    background: rgba(15, 23, 42, 0.55);
}

/* Mobil */
@media (max-width: 900px) {
    #detasSidebarFinal {
        position: relative !important;
        width: 100% !important;
        min-width: 100% !important;
        max-width: 100% !important;
        height: auto !important;
        min-height: auto !important;
        max-height: none !important;
    }

    #detasMainAreaFinal {
        margin-left: 0 !important;
        width: 100% !important;
        padding: 12px !important;
    }

    #detasContentBoxFinal {
        padding: 12px !important;
    }
}





/* =========================================================
   DETAS COMBO CONTROL PANEL
   AI Analiz + Slider + Tarama ModlarÄ± aynÄ± blokta
   ========================================================= */

.combo-control-panel {
    display: flex !important;
    flex-direction: column !important;
    gap: 14px !important;
    overflow: hidden !important;
}

.combo-section {
    background: #111827 !important;
    border: 1px solid rgba(56, 189, 248, 0.18) !important;
    border-radius: 16px !important;
    padding: 14px !important;
    box-sizing: border-box !important;
}

.combo-section-title {
    font-size: 14px !important;
    font-weight: 900 !important;
    color: #f8fafc !important;
    margin-bottom: 12px !important;
}

.combo-donut-wrap {
    margin-top: 4px !important;
    gap: 18px !important;
}

.combo-control-panel .servo-control {
    gap: 14px !important;
}

.combo-control-panel .servo-row {
    background: rgba(15, 23, 42, 0.72) !important;
    border: 1px solid rgba(56, 189, 248, 0.16) !important;
    border-radius: 14px !important;
    padding: 12px !important;
}

.combo-control-panel .servo-row strong {
    color: #f8fafc !important;
}

.combo-control-panel .servo-row span {
    background: #0f172a !important;
    color: white !important;
}

.combo-control-panel input[type="range"] {
    width: 100% !important;
}

.combo-control-panel .servo-mini-buttons {
    display: grid !important;
    grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
    gap: 8px !important;
    margin-top: 10px !important;
}

.combo-control-panel .servo-mini-buttons button {
    width: 100% !important;
    min-width: 0 !important;
    padding: 9px 8px !important;
    font-size: 12px !important;
    border-radius: 10px !important;
}

.combo-scan-buttons {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 10px !important;
    width: 100% !important;
}

.combo-scan-buttons button {
    width: 100% !important;
    min-width: 0 !important;
    box-sizing: border-box !important;
    white-space: normal !important;
    word-break: break-word !important;
    border-radius: 12px !important;
    padding: 12px 10px !important;
    font-weight: 900 !important;
}

.combo-scan-buttons button:last-child {
    grid-column: 1 / -1 !important;
}

.combo-control-panel .servo-stop-btn {
    background: linear-gradient(90deg, #991b1b, #ef4444) !important;
}

@media (max-width: 700px) {
    .combo-scan-buttons {
        grid-template-columns: 1fr !important;
    }

    .combo-scan-buttons button:last-child {
        grid-column: auto !important;
    }

    .combo-control-panel .servo-mini-buttons {
        grid-template-columns: 1fr !important;
    }
}


/* =========================================================
   DETAS KOYU TEMA DÃœZELTME
   Sadece beyazlaÅŸan kutularÄ± dÃ¼zeltir, layout'a dokunmaz
   ========================================================= */

/* Genel kartlar */
.panel,
.stat-card,
.camera-panel,
.servo-panel,
.scan-control-panel,
.scan-inner-section,
.combo-control-panel,
.combo-section,
.logs-card,
.log-panel,
.thermal-panel,
.map-panel,
.mission-panel,
.cube-panel {
    background: #15191c !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.22) !important;
}

/* Ãœst istatistik kartlarÄ± */
.stat-card,
.stat-card * {
    background-color: #15191c !important;
    color: #f8fafc !important;
}

/* GÃ¶rev durumu altÄ±ndaki kÃ¼Ã§Ã¼k kutular */
.mini-grid > div,
.thermal-stats > div,
.cube-item,
.attitude-row > div {
    background: #111827 !important;
    color: #f8fafc !important;
    border: 1px solid rgba(56, 189, 248, 0.18) !important;
}

/* Log satÄ±rlarÄ± */
.logs-list,
.logs-list > div,
.log-line {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.18) !important;
}

/* AlgÄ±lama kayÄ±tlarÄ± */
.detections-list,
.detections-list > div,
.detection-list,
.detection-list > div,
.camera-detections-wrap,
.detection-item {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.18) !important;
}

/* Servo slider kutularÄ± */
.servo-row,
.servo-control,
.scan-servo-control {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.18) !important;
}

.servo-row > div,
.servo-row strong,
.servo-row span {
    color: #f8fafc !important;
}

.servo-row span,
#panValue,
#tiltValue {
    background: #0f172a !important;
    color: #ffffff !important;
}

/* Termal Ã¼st maksimum/minimum kutularÄ± */
.thermal-stats div,
.thermal-stats span,
.thermal-stats strong {
    color: #f8fafc !important;
}

/* Grafik alanÄ± beyaz kalmasÄ±n */
.chart-box {
    background: #111827 !important;
    border: 1px solid rgba(56, 189, 248, 0.18) !important;
    border-radius: 14px !important;
    overflow: hidden !important;
}

#movementChart {
    background: #111827 !important;
}

/* Beyaz kalan genel iÃ§ kutular */
.panel div:not(.thermal-cell):not(.leaflet-container):not(.leaflet-pane):not(.leaflet-tile-container):not(.leaflet-control):not(.leaflet-control-container),
.stat-card div,
.servo-panel div,
.scan-control-panel div {
    border-color: rgba(56, 189, 248, 0.18) !important;
}

/* Beyaz input/box etkisini engelle */
input,
select,
textarea {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.22) !important;
}

/* Butonlar beyaz olmasÄ±n */
button {
    color: #ffffff !important;
}

/* Date ve Ã¼st ikonlar */
.date-box,
.icon-btn {
    background: #111827 !important;
    color: #f8fafc !important;
    border-color: rgba(56, 189, 248, 0.22) !important;
}

/* EÄŸer inline veya eski CSS yÃ¼zÃ¼nden beyaz kalan kutular varsa */
[style*="background: white"],
[style*="background:white"],
[style*="background-color: white"],
[style*="background-color:white"],
[style*="background: #fff"],
[style*="background:#fff"],
[style*="background-color: #fff"],
[style*="background-color:#fff"],
[style*="background: #ffffff"],
[style*="background:#ffffff"],
[style*="background-color: #ffffff"],
[style*="background-color:#ffffff"] {
    background: #111827 !important;
    color: #f8fafc !important;
}

/* BaÅŸlÄ±k ve metinler */
.panel h1,
.panel h2,
.panel h3,
.panel h4,
.panel p,
.panel span,
.panel small,
.panel strong,
.stat-card h3,
.stat-card span,
.stat-card small {
    color: #f8fafc !important;
}

/* Harita bozulmasÄ±n */
#map,
#map * {
    color: initial;
}




/* =====================================================
   DETAS MISSION STOP / RESET BUTTONS
   ===================================================== */

.mission-actions {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 10px !important;
    margin-top: 12px !important;
}

.mission-actions button {
    width: 100% !important;
    border: none !important;
    border-radius: 14px !important;
    padding: 12px 14px !important;
    font-weight: 900 !important;
    color: #ffffff !important;
    cursor: pointer !important;
}

.mission-stop-btn {
    background: linear-gradient(90deg, #991b1b, #ef4444) !important;
}

.mission-reset-btn {
    background: linear-gradient(90deg, #0f172a, #334155) !important;
}

.mission-actions button:hover {
    filter: brightness(1.08);
}

@media (max-width: 700px) {
    .mission-actions {
        grid-template-columns: 1fr !important;
    }
}
